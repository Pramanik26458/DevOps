kind: Service
apiVersion: v1
metadata:
  name: express-service
spec:
  selector:
    app:  express #the name selector app name must be same as the label name in the deployment.yml file
  type: ClusterIP
  ports:
  - name:  http
    port:  80
    targetPort:  3000